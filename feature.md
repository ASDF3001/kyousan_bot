# Kyousanshumi Bot 機能紹介

このBotはDiscordサーバーにおけるユーザーの活動状況を記録し、ランキング化やポイント付与などの機能を提供します。

## 1. 労働実績（活動量）の集計・ランキング
- ユーザーごとの発言数およびスレッドごとの発言数を記録します。
- `/activity_ranking` コマンドで現在のランキングを表示できます。
- `/stats` コマンドで自分自身の総発言数や全体順位を確認できます。
- 毎週金曜20時に指定チャンネルへ週間ランキングを自動投稿します。

## 2. デイリーログインボーナスとポイント機能
- 1日1回、サーバー内のいずれかのチャンネルで発言するとログインと判定されます。
- ログイン時にポイントが付与され、連続ログイン日数に応じてボーナスが増加します。
- 発言のたびにポイントが加算されますが、同一内容の連投はスパムと判定されポイントは加算されません。
- 毎月1日0時に月間ポイントがリセットされ、過去の履歴として記録されます。

## 3. あけおめ最速競争
- 受付チャンネルで対象スタンプにリアクションすることで参加します。
- 毎日0:00:00から0:01:00の間でのリアクション速度を計測し、順位付けを行います。
- 前日23:59などにフライングした場合はペナルティとして記録されます。
- 毎日0:00:30に前日の計測結果を自動投稿します。
- `/akeome_ranking` コマンドで月間の最速ランキングを確認できます。

## 4. 管理・走査機能
- **スレッド履歴の同期**: 指定されたチャンネル内のスレッドを定期的に走査し、発言数を再集計します（3日に1回、自動実行）。
- **手動走査**: `/soviet_sync` コマンドで手動による再集計を実行できます。
- **一括削除**: `/censor` コマンドで指定ユーザーのメッセージを一括削除できます。
- **設定管理**: `/setting` コマンドから、走査対象チャンネルの追加・削除や通知設定を変更できます。

## 5. おみくじ機能
- `/omikuji` コマンドで今日の運勢を占うことができます。
